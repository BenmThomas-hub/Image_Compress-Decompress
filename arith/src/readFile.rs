use Array2::Array2;
use csc411_image::*;
//use csc411_rpregio;

#[derive(Clone)]

pub struct RgbFloat{
    pub red: f32,
    pub green: f32,
    pub blue: f32
}

pub fn read(input: Option<String>) {

}